const superhero: string[] = [];
const heronumber: number[] = [];

const HeroNewWay = Array<string>();
const HeroNewWay1 = Array<number>();
const HeroNewWay2 = Array<boolean>();

superhero.push("Superman");
heronumber.push(2);

type Hero = {
  name: string;
  age: number;
  isActive: boolean;
};

const allUsers: Hero[] = [];
const allUsers1: Array<Hero> = [];

const MLModule: number[][] = [[1, 2, 3], [4, 5, 6], []];
